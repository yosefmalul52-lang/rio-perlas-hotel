/**
 * Disconnected legacy tab-based app shell.
 *
 * Preserved for reference — not mounted in the current router architecture.
 * Related modules still in the codebase but unused in navigation:
 * - src/components/RetreatBuilder.tsx
 * - src/components/ConciergeChat.tsx
 *
 * To restore temporarily, import LegacyTabApp in main.tsx instead of App.
 */

export { default as LegacyTabApp } from "./LegacyTabApp";
